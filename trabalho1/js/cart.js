const vShader2 = `#version 300 es

`;

const fShader2 = `#version 300 es
`;

function main() {
  var gl = getGLContext("canvas");
  // var program = createProgram(gl, vShader2, fShader2);
  // maybe use the same
}

main();